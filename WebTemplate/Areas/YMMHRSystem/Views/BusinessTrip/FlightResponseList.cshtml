@model List<DtoFlightReservation>
@using YMMHRSystemLogic
@{
    Layout = null;
}

<div class="dialog-content">
    <div class="row d-flex flex-align-center flex-justify-between mx-2">
        <div><h6>Flights</h6></div>
        <div><a class="button small my-button-colors" style="cursor: pointer;" onclick="OpenFlightDialog(0)"><span class="mif-plus"></span><span class="actionButton"> Add </span></a></div>
    </div>
    <!---------------------------------------TABLA ------------------------------------------>
    <table id="dttFlightResponse" class="display compact" style="width:100%">
        <thead>
            <tr>
                <th style="text-align: center">Flight No.</th>
                <th style="text-align: center">Departure Airport</th>
                <th style="text-align: center">Arrival Airport</th>
                <th style="text-align: center">Date</th>
                <th style="text-align: center">Time</th>
                <th style="text-align: center">Airline</th>
                <th style="text-align: center">Cost Company</th>
                <th style="text-align: center">Class</th>
                <th style="text-align: center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr id="@item.FlightReservationId">
                <td style="text-align: center">
                    <span> @item.FlightNumber </span>
                </td>

                <td style="text-align: center">
                    <span> @item.DepartureAirport </span>
                </td>

                <td style="text-align: center">
                    <span> @item.ArrivalAirport </span>
                </td>

                <td style="text-align: center">
                    <span> @(item.FlightDate?.ToShortDateString()) </span>
                </td>

                <td style="text-align: center">
                    <span> @item.FlightTime </span>
                </td>

                <td style="text-align: center">
                    <span> @item.Airline </span>
                </td>

                <td style="text-align: center">
                    <span> @item.CompanyPaying </span>
                </td>

                <td style="text-align: center">
                    <span> @item.Class </span>
                </td>

                <td style="text-align: center">
                    <a class="button small my-button-colors" style="cursor: pointer;" onclick="OpenFlightDialog(@item.FlightReservationId)"><span class="mif-eye"></span></a>
                    @if (HttpContext.Current.Session["CanDeleteBusinessTrip"].Equals(true))
                    {
                        <a class="button small bg-red fg-white" style="cursor: pointer;" onclick="ConfirmDeleteFlightResponse(@item.FlightReservationId)"><span class="mif-bin"></span></a>
                    }
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>

<div data-overlay="true" data-default-action="false" data-close-button="true" data-overlay-click-close="true" id="DeleteFlightResponse" name="DeleteFlightResponse" class="dialog" data-role="dialog">
    <div class="dialog-title">Delete</div>
    <div class="dialog-content"> Are you sure you want to delete this item?</div>
    <div class="dialog-actions ml-auto">
        <button class="button my-control-colors" onclick="DeleteFlightResponse()"> Yes</button>
        <button class="button alert" onclick="Metro.dialog.close('#DeleteFlightResponse')"> No</button>
    </div>
</div>

<script>
    var tableFlight;
    $(document).ready(function () {

        tableFlight = $('#dttFlightResponse').DataTable({
            retrieve: true,
            responsive: true,
            searching: false,
            info: false,
            paging: false,
            ordering: false,
            scrollY: "20vh",
            scrollCollapse: true,
            initComplete: function () {
                $("#FlightResponseTable").show();
            }
        });

    });


    function DeleteFlightResponse() {
        var aux;
        $("#dttFlightResponse tr").each(function () {
            if ($(this).attr("id") == window.$FlightId) {
                aux = $(this);
            }
        });

        $.ajax({
            method: "POST",
            url: window.$DeleteFlight,
            data: { flightReservationId: window.$FlightId },
            success: function (result) {
                if (result === "true") {
                    Metro.toast.create("Flight deleted.", null, null, "bg-green fg-white");
                    setTimeout(function () { location.reload(); }, 1000);
                } else {
                    Metro.toast.create("Error deleting the flight or business trip is complete.", null, null, "bg-red fg-white");
                }
                Metro.dialog.close('#DeleteFlightResponse')
            }
        });
    }

    function ConfirmDeleteFlightResponse(flightId) {
        var dialog = Metro.getPlugin('#DeleteFlightResponse', 'dialog');
        dialog.open();
        window.$FlightId = flightId;
    }
</script>


